{% extends 'back/base.html.twig' %}

{% block title %}
    {{ brand.name }}
{% endblock %}

{% block body %}
    <h1>{{ brand.name }}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Nom</th>
            <td>{{ brand.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_brand_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_brand_edit', {'slug': brand.slug}) }}">Modifier</a>

    {{ include('back/brand/_delete_form.html.twig') }}
{% endblock %}
