{% extends 'back/base.html.twig' %}

{% block title %}
    {{ category.name }}
{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Nom</th>
            <td>{{ category.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_category_edit', {'slug': category.slug}) }}">Modifier</a>

    {{ include('back/category/_delete_form.html.twig') }}
{% endblock %}
