{% extends 'back/base.html.twig' %}

{% block title %}
    {{ payment_method.name }}
{% endblock %}

{% block body %}
    <h1>{{ payment_method.name }}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Nom</th>
            <td>{{ payment_method.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_payment_method_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_payment_method_edit', {'slug': payment_method.slug}) }}">Modifier</a>

    {{ include('back/payment_method/_delete_form.html.twig') }}
{% endblock %}
